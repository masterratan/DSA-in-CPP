// Nothing but a shortcut of If Else
// Format-> condition ? value if true : value if false

#include<iostream>
using namespace std;
int main(){
    int x;
    cin>>x;
    // if(x%2==0) cout<<"even";
    // else cout<<"odd";

    // Or,

    x%2==0 ? cout<<"even" : cout<<"odd";
}